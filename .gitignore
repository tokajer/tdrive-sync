# Build output
/build/
/dist/

# Go
*.test
*.out
/vendor/

# Editor / tooling
# Ignore the contents, not the directory itself, so the skill below can be
# re-included – git does not descend into an excluded directory.
/.claude/*
# The project skill is documentation and belongs in the repository.
!/.claude/skills/
/.vscode/
/.idea/
*.swp
*.swo
*~
.direnv/
.env
.env.local

# OS
.DS_Store
Thumbs.db
desktop.ini
